package;

import sunaba.App;

class Main extends App {
    static function main() {
        new Main();
    }

    override function init() {
        try {
        }
        catch(e) {
            trace(e);
        }
    }
}
